import { Auto } from "./autos";

class AutoClasico extends Auto {
    color: string;

    constructor(color: string, velocidad: number, cambio: number) {
        super(velocidad, cambio,)
        this.color = color;
    }



}